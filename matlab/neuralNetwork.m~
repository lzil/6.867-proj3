function [ output_args ] = neuralNetwork( m, X, W1, W1_0, W2, W2_0 )
%neuralNetwork Implements a neural network
%   m: number of hidden layers
%   X: input layer, d x 1
%   W1: weights 1, m x d
%   W1_0: weight 1 bias, m x 1
%   W2: weights 2, k x m
%   W2_0: weight 2 bias, k x 1

d = length(X);
for j = 1:d
    a = 

end

function [ out ] = sigmoid( z )
out = 1 / (1 + exp(-z));
end